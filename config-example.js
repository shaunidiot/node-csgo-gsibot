var config = {};

config.admin = ''; // the main account playing CS:GO
config.c4buffer = 1; // buffer time to compensate the lag

config.bot = {
    username: '', //steambot username
    password: '', //steambot password
    authCode: '' //steambot email auth
};

module.exports = config;
